"""RetAlert transport layer.

Step 1 implements ``identity`` and ``lxmf_transport``. ``rns_link`` (raw RNS
Link for live media) and ``share_instance`` (attach to Sideband/Columba/
MeshChat/MeshChatX) are stubs for later build steps.
"""